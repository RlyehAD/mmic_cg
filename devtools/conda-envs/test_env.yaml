name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - qcelemental
    - codecov
    - git+https://github.com/MolSSI/cmselemental@v0.2.0
    - git+https://github.com/RlyehAD/mmelemental
    - git+https://github.com/MolSSI/mmic
