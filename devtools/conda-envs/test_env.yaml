name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - qcelemental
    - codecov
    - git+https://github.com/MolSSI/cmselemental@dev
    - git+https://github.com/MolSSI/mmelemental

